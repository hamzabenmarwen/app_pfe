export default {
  plugins: {
    // Tailwind CSS is handled by @tailwindcss/vite plugin in vite.config.ts
    // Do NOT add @tailwindcss/postcss here — it conflicts with the Vite plugin.
    autoprefixer: {},
  },
}
